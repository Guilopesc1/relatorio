# Docker Compose - Versão Final Limpa para EasyPanel
version: '3.8'

services:
  facebook-reports:
    build:
      context: ../..
      dockerfile: production/docker/Dockerfile.final
    container_name: facebook-reports
    restart: unless-stopped
    
    environment:
      - WEB_HOST=0.0.0.0
      - WEB_PORT=5000
      - ENABLE_CRON=${ENABLE_CRON:-false}
      
      # Obrigatórias
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
      - FACEBOOK_ACCESS_TOKEN=${FACEBOOK_ACCESS_TOKEN}
      
      # Opcionais
      - FACEBOOK_APP_ID=${FACEBOOK_APP_ID:-}
      - FACEBOOK_APP_SECRET=${FACEBOOK_APP_SECRET:-}
      - GOOGLE_ADS_DEVELOPER_TOKEN=${GOOGLE_ADS_DEVELOPER_TOKEN:-}
      - GOOGLE_ADS_CLIENT_ID=${GOOGLE_ADS_CLIENT_ID:-}
      - GOOGLE_ADS_CLIENT_SECRET=${GOOGLE_ADS_CLIENT_SECRET:-}
      - GOOGLE_ADS_REFRESH_TOKEN=${GOOGLE_ADS_REFRESH_TOKEN:-}
      - GOOGLE_ADS_LOGIN_CUSTOMER_ID=${GOOGLE_ADS_LOGIN_CUSTOMER_ID:-}
      - EVOLUTION_API_URL=${EVOLUTION_API_URL:-}
      - EVOLUTION_API_TOKEN=${EVOLUTION_API_TOKEN:-}
      - EVOLUTION_INSTANCE=${EVOLUTION_INSTANCE:-}
      - DAILY_UPDATE_HOUR=${DAILY_UPDATE_HOUR:-6}
    
    ports:
      - "5000:5000"
    
    volumes:
      - facebook-logs:/app/logs
    
    healthcheck:
      test: ["CMD", "/healthcheck.sh"]
      interval: 60s
      timeout: 10s
      retries: 3

volumes:
  facebook-logs:
