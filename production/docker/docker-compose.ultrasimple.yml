# Docker Compose ULTRA SIMPLES para EasyPanel
version: '3.8'

services:
  facebook-reports:
    build:
      context: ../..
      dockerfile: production/docker/Dockerfile.ultrasimple
    container_name: facebook-reports-simple
    restart: unless-stopped
    
    environment:
      # Essenciais
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
      - FACEBOOK_ACCESS_TOKEN=${FACEBOOK_ACCESS_TOKEN}
      
      # Configuração da aplicação
      - WEB_HOST=0.0.0.0
      - WEB_PORT=5000
      - DEBUG=false
      
      # Opcionais
      - FACEBOOK_APP_ID=${FACEBOOK_APP_ID:-}
      - FACEBOOK_APP_SECRET=${FACEBOOK_APP_SECRET:-}
      - GOOGLE_ADS_DEVELOPER_TOKEN=${GOOGLE_ADS_DEVELOPER_TOKEN:-}
      - GOOGLE_ADS_CLIENT_ID=${GOOGLE_ADS_CLIENT_ID:-}
      - GOOGLE_ADS_CLIENT_SECRET=${GOOGLE_ADS_CLIENT_SECRET:-}
      - GOOGLE_ADS_REFRESH_TOKEN=${GOOGLE_ADS_REFRESH_TOKEN:-}
      - GOOGLE_ADS_LOGIN_CUSTOMER_ID=${GOOGLE_ADS_LOGIN_CUSTOMER_ID:-}
      - EVOLUTION_API_URL=${EVOLUTION_API_URL:-}
      - EVOLUTION_API_TOKEN=${EVOLUTION_API_TOKEN:-}
      - EVOLUTION_INSTANCE=${EVOLUTION_INSTANCE:-}
    
    ports:
      - "5000:5000"
    
    volumes:
      - facebook-reports-logs:/app/logs
    
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 30s

volumes:
  facebook-reports-logs:
    driver: local
